import * as React from "react";

import CustomCheckbox from "./CustomCheckbox";

export default {
  title: "Input",
  component: CustomCheckbox
};

export const _CustomCheckbox = () => <CustomCheckbox />;
