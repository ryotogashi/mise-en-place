import * as React from "react";

import CustomRadioButton from "./CustomRadioButton";

export default {
  title: "Input",
  component: CustomRadioButton
};

export const _CustomRadioButton = () => <CustomRadioButton />;
